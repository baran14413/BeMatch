{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user profile in the Nexus dating app.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "email": {
          "type": "string",
          "description": "Email address of the user.",
          "format": "email"
        },
        "phoneNumber": {
          "type": "string",
          "description": "Phone number of the user."
        },
        "firstName": {
          "type": "string",
          "description": "User's first name."
        },
        "lastName": {
          "type": "string",
          "description": "User's last name."
        },
        "name": {
            "type": "string",
            "description": "User's full display name."
        },
        "age": {
            "type": "number",
            "description": "Age of the user."
        },
        "dateOfBirth": {
          "type": "string",
          "description": "Date of birth of the user.",
          "format": "date-time"
        },
        "gender": {
          "type": "string",
          "description": "Gender of the user."
        },
        "interestedIn": {
          "type": "string",
          "description": "Gender(s) the user is interested in."
        },
        "location": {
          "type": "string",
          "description": "The user's current location (e.g., city, state)."
        },
        "latitude": {
            "type": "number",
            "description": "The latitude of the user's location."
        },
        "longitude": {
            "type": "number",
            "description": "The longitude of the user's location."
        },
        "lastSeen": {
          "type": "string",
          "format": "date-time",
          "description": "The last time the user was active."
        },
        "passwordLastUpdatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "The last time the user's password was updated."
        },
        "lastRewindAt": {
            "type": "string",
            "format": "date-time",
            "description": "The last time the user used the rewind feature."
        },
        "rewindCount": {
            "type": "number",
            "description": "The number of rewinds a user has used today."
        },
        "bio": {
          "type": "string",
          "description": "User's biography or 'about me' section."
        },
        "instagramHandle": {
          "type": "string",
          "description": "User's Instagram handle."
        },
        "avatarUrl": {
          "type": "string",
          "description": "URL of the user's primary profile picture.",
          "format": "uri"
        },
        "imageUrls": {
            "type": "array",
            "description": "An array of URLs for the user's gallery images.",
            "items": {
                "type": "string",
                "format": "uri"
            }
        },
        "videoUrl": {
          "type": "string",
          "description": "URL of the user's introductory video.",
          "format": "uri"
        },
        "voiceNoteUrl": {
          "type": "string",
          "description": "URL of the user's voice note.",
          "format": "uri"
        },
        "promptAnswerIds": {
          "type": "array",
          "description": "References to PromptAnswers. (Relationship: User 1:N PromptAnswer)",
          "items": {
            "type": "string"
          }
        },
        "compatibilityRadarChartId": {
          "type": "string",
          "description": "Reference to CompatibilityRadarChart. (Relationship: User 1:1 CompatibilityRadarChart)"
        },
        "globalMode": {
            "type": "boolean",
            "description": "Whether the user wants to see profiles from all over the world, sorted by distance."
        },
        "maxDistance": {
            "type": "number",
            "description": "The maximum distance in kilometers for discovery."
        },
        "ageRange": {
            "type": "array",
            "description": "The preferred age range for discovery.",
            "items": {
                "type": "number"
            }
        },
        "isPremium": {
          "type": "boolean",
          "description": "Indicates if the user has an active premium subscription."
        },
        "subscriptionId": {
            "type": "string",
            "description": "The product ID of the user's current subscription (e.g., 'monthly-base')."
        },
        "purchaseToken": {
            "type": "string",
            "description": "The Google Play purchase token for the current subscription, used for validation."
        },
        "expiryDate": {
            "type": "string",
            "format": "date-time",
            "description": "The expiration date of the user's current subscription."
        },
        "autoRenewing": {
            "type": "boolean",
            "description": "Indicates if the subscription is set to auto-renew."
        },
        "premiumTier": {
            "type": "string",
            "enum": ["plus", "gold", "platinum", "weekly"],
            "description": "The premium subscription tier of the user."
        },
        "premiumExpiresAt": {
            "type": "string",
            "format": "date-time",
            "description": "The expiration date of the user's premium subscription."
        },
        "boostExpiresAt": {
            "type": "string",
            "format": "date-time",
            "description": "The expiration date of the user's profile boost."
        },
        "superLikes": {
            "type": "number",
            "description": "The number of Super Likes a user has."
        },
        "role": {
          "type": "string",
          "description": "The user's role in the system (e.g., admin, user)."
        },
        "isBanned": {
          "type": "boolean",
          "description": "Indicates if the user account is banned from accessing the application."
        }
      },
      "required": [
        "id", "firstName", "lastName", "name", "email", "age", "dateOfBirth", "gender", "interestedIn"
      ]
    },
    "Match": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Match",
      "type": "object",
      "description": "Represents a match or potential chat between two users.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Match entity."
        },
        "users": {
          "type": "array",
          "description": "Array containing the UIDs of both matched users.",
          "items": {
            "type": "string"
          }
        },
        "lastMessage": {
          "type": "string",
          "description": "The last message sent in the chat."
        },
        "timestamp": {
          "type": "string",
          "description": "Timestamp of the last message sent.",
          "format": "date-time"
        },
        "isBlocked": {
            "type": "boolean",
            "description": "Indicates if the match is blocked by one of the users."
        },
        "blockedBy": {
            "type": "string",
            "description": "The UID of the user who initiated the block."
        }
      },
      "required": [
        "id",
        "users",
        "timestamp"
      ],
      "patternProperties": {
        "^user_info_": {
          "type": "object",
          "properties": {
            "name": { "type": "string" },
            "avatarUrl": { "type": "string", "format": "uri" }
          },
          "required": ["name", "avatarUrl"],
          "description": "Denormalized user data for efficient reads in the lounge."
        }
      }
    },
    "Swipe": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Swipe",
      "type": "object",
      "description": "Represents a swipe action (like, nope, superlike) from one user to another.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "like",
            "nope",
            "superlike"
          ],
          "description": "The type of swipe action."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "When the swipe occurred."
        },
        "likerId": {
          "type": "string",
          "description": "The UID of the user who performed the swipe."
        },
        "likerName": {
            "type": "string",
            "description": "Denormalized name of the liker to prevent extra reads."
        },
        "likerAvatar": {
            "type": "string",
            "description": "Denormalized avatar URL of the liker to prevent extra reads."
        }
      },
      "required": [
        "type",
        "timestamp",
        "likerId",
        "likerName",
        "likerAvatar"
      ]
    },
    "Message": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Message",
      "type": "object",
      "description": "Represents a chat message between two users.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Message entity."
        },
        "senderId": {
          "type": "string",
          "description": "Reference to User who sent the message. (Relationship: User 1:N Message)"
        },
        "text": {
          "type": "string",
          "description": "Content of the message."
        },
        "timestamp": {
          "type": "string",
          "description": "Timestamp of when the message was sent.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp of when the message was last updated.",
          "format": "date-time"
        },
        "isEdited": {
          "type": "boolean",
          "description": "Indicates if the message has been edited."
        },
        "isAiGenerated": {
          "type": "boolean",
          "description": "Indicates whether the message was generated by AI."
        },
        "reactions": {
            "type": "object",
            "description": "A map of emoji reactions to an array of user IDs.",
            "additionalProperties": {
                "type": "array",
                "items": { "type": "string" }
            }
        },
        "isRead": {
          "type": "boolean",
          "description": "Indicates if the message has been read by the recipient."
        }
      },
      "required": [
        "senderId",
        "text",
        "timestamp"
      ]
    },
    "Prompt": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Prompt",
      "type": "object",
      "description": "Represents a prompt question for users to answer on their profile.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Prompt entity."
        },
        "question": {
          "type": "string",
          "description": "The text of the prompt question."
        }
      },
      "required": [
        "id",
        "question"
      ]
    },
    "PromptAnswer": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "PromptAnswer",
      "type": "object",
      "description": "Represents a user's answer to a specific prompt.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the PromptAnswer entity."
        },
        "promptId": {
          "type": "string",
          "description": "Reference to Prompt. (Relationship: Prompt 1:N PromptAnswer)"
        },
        "answer": {
          "type": "string",
          "description": "The user's answer to the prompt."
        },
        "likes": {
          "type": "number",
          "description": "Number of likes the prompt answer has received."
        }
      },
      "required": [
        "id",
        "promptId",
        "answer"
      ]
    },
    "CompatibilityRadarChart": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "CompatibilityRadarChart",
      "type": "object",
      "description": "Represents the data for a compatibility radar chart comparing two users.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the CompatibilityRadarChart entity."
        },
        "trait1": {
          "type": "number",
          "description": "Value for trait 1."
        },
        "trait2": {
          "type": "number",
          "description": "Value for trait 2."
        },
        "trait3": {
          "type": "number",
          "description": "Value for trait 3."
        },
        "trait4": {
          "type": "number",
          "description": "Value for trait 4."
        },
        "trait5": {
          "type": "number",
          "description": "Value for trait 5."
        }
      },
      "required": [
        "id"
      ]
    },
    "Report": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Report",
      "type": "object",
      "description": "Represents a user-submitted report against another user.",
      "properties": {
        "id": { "type": "string", "description": "Unique ID for the report." },
        "reporterId": { "type": "string", "description": "The UID of the user who submitted the report." },
        "reportedUserId": { "type": "string", "description": "The UID of the user being reported." },
        "matchId": { "type": "string", "description": "The ID of the match where the report originated, if any." },
        "reason": { "type": "string", "description": "The category/reason for the report." },
        "description": { "type": "string", "description": "A detailed description of the incident." },
        "timestamp": { "type": "string", "format": "date-time", "description": "When the report was submitted." },
        "status": { "type": "string", "enum": ["pending", "reviewed", "resolved"], "description": "The current status of the report." }
      },
      "required": ["id", "reporterId", "reportedUserId", "reason", "timestamp", "status"]
    },
    "VoiceRoom": {
        "$schema": "http://json-schema.org/draft-07/schema#",
        "title": "VoiceRoom",
        "type": "object",
        "description": "Represents a voice chat room.",
        "properties": {
            "id": { "type": "string", "description": "Unique ID for the voice room." },
            "title": { "type": "string", "description": "The title of the voice room." },
            "ownerId": { "type": "string", "description": "The UID of the user who owns the room." },
            "isPublic": { "type": "boolean", "description": "True if the room is public, false if it's password-protected." },
            "passwordHash": { "type": "string", "description": "Hashed password for private rooms. Optional." },
            "participantCount": { "type": "number", "description": "The current number of participants." },
            "maxParticipants": { "type": "number", "description": "The maximum number of participants allowed." },
            "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp when the room was created." },
            "bannedUserIds": { "type": "array", "items": { "type": "string" }, "description": "A list of UIDs of users banned from this room." }
        },
        "required": ["id", "title", "ownerId", "isPublic", "participantCount", "maxParticipants", "createdAt"]
    },
    "WebRTCSignal": {
        "$schema": "http://json-schema.org/draft-07/schema#",
        "title": "WebRTCSignal",
        "type": "object",
        "description": "Represents a WebRTC signaling message (offer, answer, or ICE candidate) for a specific user in a room.",
        "properties": {
            "type": { "type": "string", "enum": ["offer", "answer", "candidate"], "description": "The type of WebRTC signal." },
            "sdp": { "type": "string", "description": "The Session Description Protocol (SDP) string for 'offer' and 'answer' types." },
            "candidate": { "type": "object", "description": "The ICE candidate object for 'candidate' types." }
        },
        "required": ["type"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ],
    "customClaims": {
      "role": {
        "type": "string",
        "enum": ["admin", "moderator", "support", "user"]
      }
    },
    "roles": {
      "admin": {
        "name": "Yönetici",
        "description": "Tüm yönetici paneli özelliklerine tam erişime sahiptir.",
        "permissions": ["*"]
      },
      "moderator": {
        "name": "Moderatör",
        "description": "Kullanıcıları yönetebilir ve rapor edilen içerikleri inceleyebilir.",
        "permissions": [
          "/admin/users",
          "/admin/safety",
          "/admin/audit-logs"
        ]
      },
      "support": {
        "name": "Destek",
        "description": "Destek biletlerine yardımcı olmak için kullanıcı bilgilerini görüntüleyebilir.",
        "permissions": [
          "/admin/users"
        ]
      },
      "user": {
        "name": "Kullanıcı",
        "description": "Yönetici paneli erişimi olmayan standart kullanıcı.",
        "permissions": []
      }
    }
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profile information. Path-based ownership ensures only the user can access their data.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/swipes/{targetUserId}",
        "definition": {
          "entityName": "Swipe",
          "schema": {
            "$ref": "#/backend/entities/Swipe"
          },
          "description": "Stores the current user's swipe actions on other users.",
          "params": [
            {
              "name": "userId",
              "description": "The UID of the user performing the swipe."
            },
            {
              "name": "targetUserId",
              "description": "The UID of the user being swiped on."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/likedBy/{likerId}",
        "definition": {
          "entityName": "Swipe",
          "schema": {
            "$ref": "#/backend/entities/Swipe"
          },
          "description": "Stores who has liked the current user. Used for the 'Likes You' screen.",
          "params": [
            {
              "name": "userId",
              "description": "The UID of the user who is being liked."
            },
            {
              "name": "likerId",
              "description": "The UID of the user who performed the like."
            }
          ]
        }
      },
      {
        "path": "/matches/{matchId}",
        "definition": {
          "entityName": "Match",
          "schema": {
            "$ref": "#/backend/entities/Match"
          },
          "description": "Stores match information between two users, acting as a chat room.",
          "params": [
            {
              "name": "matchId",
              "description": "The unique identifier of the match, typically a combination of the two user IDs."
            }
          ]
        }
      },
      {
        "path": "/matches/{matchId}/messages/{messageId}",
        "definition": {
          "entityName": "Message",
          "schema": {
            "$ref": "#/backend/entities/Message"
          },
          "description": "Stores messages within a specific match. Access is restricted to users involved in the match.",
          "params": [
            {
              "name": "matchId",
              "description": "The unique identifier of the match."
            },
            {
              "name": "messageId",
              "description": "The unique identifier of the message."
            }
          ]
        }
      },
      {
        "path": "/prompts/{promptId}",
        "definition": {
          "entityName": "Prompt",
          "schema": {
            "$ref": "#/backend/entities/Prompt"
          },
          "description": "Stores predefined prompt questions.",
          "params": [
            {
              "name": "promptId",
              "description": "The unique identifier of the prompt."
            }
          ]
        }
      },
      {
        "path": "/compatibilityRadarCharts/{compatibilityRadarChartId}",
        "definition": {
          "entityName": "CompatibilityRadarChart",
          "schema": {
            "$ref": "#/backend/entities/CompatibilityRadarChart"
          },
          "description": "Stores compatibility radar chart data.",
          "params": [
            {
              "name": "compatibilityRadarChartId",
              "description": "The unique identifier of the compatibility radar chart."
            }
          ]
        }
      },
       {
        "path": "/reports/{reportId}",
        "definition": {
          "entityName": "Report",
          "schema": { "$ref": "#/backend/entities/Report" },
          "description": "Stores user-submitted reports for moderation.",
          "params": [
            { "name": "reportId", "description": "The unique ID of the report." }
          ]
        }
      },
      {
        "path": "/voiceRooms/{roomId}",
        "definition": {
            "entityName": "VoiceRoom",
            "schema": { "$ref": "#/entities/VoiceRoom" },
            "description": "Stores metadata for a voice chat room.",
            "params": [
                { "name": "roomId", "description": "The unique ID of the voice room." }
            ]
        }
      },
      {
        "path": "/voiceRooms/{roomId}/participants/{userId}",
        "definition": {
            "entityName": "User",
            "schema": { "$ref": "#/entities/User" },
            "description": "Stores a snapshot of users currently in a voice room.",
            "params": [
                { "name": "roomId", "description": "The unique ID of the voice room." },
                { "name": "userId", "description": "The unique ID of the participant." }
            ]
        }
      },
      {
        "path": "/voiceRooms/{roomId}/messages/{messageId}",
        "definition": {
          "entityName": "Message",
          "schema": { "$ref": "#/entities/Message" },
          "description": "Stores messages within a specific voice room.",
          "params": [
            { "name": "roomId", "description": "The unique ID of the voice room." },
            { "name": "messageId", "description": "The unique ID of the message." }
          ]
        }
      }
    ],
    "reasoning": "This Firestore structure is designed to support the Nexus dating app's core features while adhering to the principles of Authorization Independence, Clarity of Intent, DBAC (Database-Based Access Control), and QAPs (Rules are not Filters). It prioritizes security, scalability, and debuggability.\\n\\n**Authorization Independence via Denormalization:**\\n*   User profiles are stored under `/users/{userId}`. Data needing authorization context from the user profile is denormalized to avoid `get()` calls in security rules. For example, prompt answers created by a user are stored in a subcollection `/users/{userId}/promptAnswers/{promptAnswerId}`. The prompt answer documents do not require any authorization data copied because access is solely based on path and `request.auth.uid`.\\n*   Matches between users are stored in a top-level `/matches/{matchId}` collection, which contains an array of `users`. Messages sent within a match are stored in a subcollection `/matches/{matchId}/messages/{messageId}`.  The message documents do not require any authorization data copied because access is solely based on path and `request.auth.uid`.\\n\\n**Structural Segregation (Homogeneous Security Posture):**\\n*   Private user data (profiles, prompt answers, matches, and messages) is kept separate from potentially public data. This separation ensures that each collection has a clear and consistent security posture, avoiding the need for complex logic within security rules.\\n\\n**Access Modeling (Standardization and Consistency):**\\n*   **Private Data:** User profiles and their associated data (prompt answers, swipes) utilize the path-based ownership pattern `/users/{userId}/*`. This approach allows straightforward security rules based on `request.auth.uid == userId`.\\n*   **Collaborative Data:** Matches and messages are secured using the `users` array in the parent match document."
  },
  "database": {
      "rules": "database.rules.json",
      "structure": [
          {
              "path": "/status/{userId}",
              "description": "Tracks the online/offline presence of users."
          },
          {
              "path": "/webrtcSignals/{roomId}/{userId}",
              "definition": {
                  "entityName": "WebRTCSignal",
                  "schema": { "$ref": "#/entities/WebRTCSignal" },
                  "description": "Used for WebRTC signaling (offers, answers, ICE candidates) within a specific voice room."
              }
          }
      ]
  }
}
