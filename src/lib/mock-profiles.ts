'use client';
import type { UserProfile } from './data';

// This file can now be safely removed or kept for testing purposes.
// The new system in onboarding-context.tsx dynamically generates names and uses placeholder images.
export const mockProfiles: UserProfile[] = [];
